{
  "name": "leanpub-monitoring",
  "version": "1.0.0",
  "description": "Example monitoring project with fp-ts and aws lambda",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf dist",
    "webpack": "webpack",
    "zip": "cd dist && zip -r cloudwatch.zip *",
    "build": "npm i && webpack && npm run zip",
    "test": "jest --coverage"
  },
  "dependencies": {
    "fp-ts": "^2.8.3",
    "fp-ts-std": "^0.8.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.764.0",
    "jest": "^26.0.0",
    "ts-jest": "^26.0.0",
    "ts-loader": "^5.3.3",
    "typescript": "4.6.4",
    "webpack": "^5.3.0",
    "webpack-cli": "^4.1.0"
  },
  "author": "Sam Van Overmeire",
  "license": "ISC"
}
